### Start the project by following these steps:

1. Create .env-file with JWT_SECRET=your_secret and PORT=5000
2. Run mongodb server on URI: mongodb://localhost:27017
3. Run npm install in both client and server folders.
4. Run npm start or node app.js in server folder.
5. Run npm run dev in client folder.

### How to use:

1. Press the signup button and create a new user.
2. Log in with the created user.
3. Press the notes-tab and create note(s), try editing or removing them.
4. Press the username and sign out.

Videogenomgång:
https://www.youtube.com/watch?v=4jhFk3yTitQ
